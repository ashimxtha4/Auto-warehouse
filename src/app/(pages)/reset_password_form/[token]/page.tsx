import React from 'react'
import type { Metadata } from 'next'
import { PageMetadata } from '@/utils/meta-data'

export const metadata: Metadata = PageMetadata('Reset Password Form', 'Forgot your password? Reset it here.')

const Page = () => {
  return (
    <></>
  )
}

export default Page
